---
name: confirm_win_target_or_form_one
description: Target actual WIN objects; if none reachable, commit to forming '<NOUN> IS WIN'.
---

Before moving, inspect active rules to list all nouns with the WIN property. Translate those nouns into actual in-level f-objects (targets). If such f-objects exist, treat them as navigation targets; ignore the 'win' text block itself as a destination.

Quick reachability check: if the nearest WIN f-object lies behind a continuous STOP barrier (e.g., a wall column) with no gap, do not keep walking toward it. Instead, switch modes to rule-making.

Rule-making mode: Prefer completing an existing 'IS WIN' anchor. Select the closest movable noun-text (not part of 'BABA IS YOU') as the subject. Plan a safe approach to the correct push side, then attach the noun to 'IS WIN' to create a new WIN condition.

Safeguards: Avoid disrupting 'BABA IS YOU'. If any planned push risks breaking control, re-route or pick a different noun. After any change, re-evaluate WIN targets and resume direct navigation only when a reachable WIN object exists.
